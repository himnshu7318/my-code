class Solution:
    def leaders(self, arr):
        # code here
        n = len(arr)
        m=[]
        k=0
        for i in range(n-1,-1,-1):
            if arr[i]>=k:
                k=arr[i]
                m.append(k)
        m.reverse()
               
        return m
                
